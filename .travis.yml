language: cpp
sudo: false
compiler:
  - gcc
  - clang
os: linux
script: cd simulator; make mipt-mips disasm test

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-5.2
    - libelf-dev
    - libboost-all-dev
    - libc++-dev
